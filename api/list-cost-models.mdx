---
title: "List Cost Models"
api: "GET /cost-models"
---

List all cost models for the current organization.

**Auth:** `sk_` only

<Warning>
All cost model endpoints require a secret key (`sk_`) and return only models owned by that key's organization. Publishable keys and customer sessions receive a 403 Forbidden response.
</Warning>

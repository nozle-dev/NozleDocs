---
title: "Cancel Subscription"
api: "DELETE /subscriptions/{id}"
---

Cancel a subscription. The cancellation takes effect at the end of the current billing period -- the customer retains access until then.

**Auth:** customer session with `subscriptions:write`, or a server-side secret key.

<ParamField path="id" type="string" required>
  The subscription ID to cancel.
</ParamField>

<ParamField body="reason" type="string">
  Optional cancellation reason for your records.
</ParamField>

<ParamField body="customer_id" type="string" required>
  External customer ID. For a customer session this must match the session subject.
</ParamField>

Nozle verifies both the customer and subscription ownership in the authenticated organization before cancellation. A subscription ID from another customer or organization is rejected.

### Response

Returns the updated subscription with a `canceled` status and the `ends_at` timestamp indicating when access expires.
